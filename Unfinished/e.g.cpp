//What should put in this folder? For example:
#include <bits/stdc++.h>
using namespace std;

const int NR = 1e6;
int n,a[NR];

int main()
{
    cin>>n;
    for(int i = 0;i < n;i++)
        cin>>a[i];
    sort(a,a + n);
    //The output wasn't finished, so we put it in this folder temporary
    return 0;
}
