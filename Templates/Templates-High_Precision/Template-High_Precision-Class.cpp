class ii{
private:
	string a;
public:
	string operator+(const ii b)const{
		string ans;
		int na[L] = {0},nb[L] = {0};
		int la = a.size(),lb = b.a.size();
		for(int i = 0;i < la;i++)
			na[la - 1 - i] = a[i] - '0';
		for(int i = 0;)
	}
};